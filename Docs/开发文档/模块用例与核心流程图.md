# 《时光回序》模块用例与核心流程图（MVP）

## 1. 文档说明

本文档基于现有资料整理：
- 需求与设计：`需求分析.md`、`设计文档.md`
- 范围与规则：`MVP开发说明.md`、`MVP开发TODO.md`
- 接口边界：`接口清单文档.md`
- 数据与状态：`数据库设计文档.md`

统一角色定义：
- 普通用户：使用 `/api/**` 的业务用户
- 管理员：使用 `/admin/**` 的后台管理用户
- 定时任务：系统调度器，执行自动解锁
- AI服务：写作提示与内容整理能力
- 微信消息服务：解锁后订阅提醒（已授权用户）

---

## 2. 用户模块

### 2.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> UC1((注册))
    U --> UC2((登录))
    U --> UC3((获取当前用户信息))
    U --> UC4((修改个人资料))
```

### 2.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 注册 | 普通用户 | 用户名未被占用 | 生成用户账号 | 提交用户名/密码/昵称，系统校验并创建用户 | 用户名重复返回业务错误 |
| 登录 | 普通用户 | 账号存在且状态为 ENABLED | 返回 JWT 与用户信息 | 提交账号密码，校验成功后签发 token | 状态 DISABLED 禁止登录 |
| 获取当前用户信息 | 普通用户 | 已登录且 token 有效 | 返回当前用户详情 | 通过 token 识别 userId，查询并返回资料 | token 无效返回未登录错误 |
| 修改个人资料 | 普通用户 | 已登录 | 用户资料更新成功 | 更新昵称/邮箱/头像 | 不能修改用户名和密码 |

### 2.3 核心业务流程图（顺序图：登录）

```mermaid
sequenceDiagram
    participant U as 普通用户
    participant API as AuthController
    participant S as AuthService
    participant DB as user表
    participant JWT as JwtTokenProvider

    U->>API: POST /api/auth/login\nusername,password
    API->>S: 登录请求
    S->>DB: 按username查询用户
    DB-->>S: 用户记录
    S->>S: 校验密码与状态
    S->>JWT: 生成token(userId, role)
    JWT-->>S: token
    S-->>API: 登录结果(token + user)
    API-->>U: ApiResponse.success(data)
```

---

## 3. 记录模块

### 3.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> RC1((创建草稿))
    U --> RC2((编辑草稿))
    U --> RC3((删除草稿))
    U --> RC4((封存记录))
    U --> RC5((我的记录列表))
    U --> RC6((记录详情))
```

### 3.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 创建草稿 | 普通用户 | 已登录 | record.status=DRAFT | 提交内容、类型、标签等信息创建记录 | 记录归属必须为当前用户 |
| 编辑草稿 | 普通用户 | 已登录且记录归属本人 | 草稿内容更新 | 修改标题/正文/标签/解锁时间 | 仅 DRAFT 可编辑 |
| 删除草稿 | 普通用户 | 已登录且记录归属本人 | 草稿被删除 | 删除指定草稿记录 | 仅 DRAFT 可删除 |
| 封存记录 | 普通用户 | 已登录且记录归属本人 | 状态变更为 SEALED | 提交 unlockAt，系统封存并写 sealedAt | unlockAt 必须晚于当前时间 |
| 我的记录列表 | 普通用户 | 已登录 | 返回分页记录 | 按状态/类型/标签/关键字筛选 | 仅返回当前用户数据 |
| 记录详情 | 普通用户 | 已登录且记录归属本人 | 返回详情与可回信标记 | 查询单条记录及标签等信息 | SEALED 也允许本人查看 |

### 3.3 核心业务流程图（状态图：记录生命周期）

```mermaid
stateDiagram-v2
    [*] --> DRAFT: 创建草稿
    DRAFT --> DRAFT: 编辑草稿
    DRAFT --> SEALED: 封存记录\nunlockAt>now
    DRAFT --> [*]: 删除草稿
    SEALED --> UNLOCKED: 定时任务解锁\nunlockAt<=now
    UNLOCKED --> [*]: 查看/回信
```

---

## 4. 时间模块

### 4.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> TM1((设置解锁时间))
    U --> TM2((查看已解锁记录))
    J[定时任务] --> TM3((自动解锁到期记录))
    W[微信消息服务] --> TM4((发送解锁提醒))
```

### 4.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 设置解锁时间 | 普通用户 | 记录为 DRAFT 且归属本人 | 记录具备 unlockAt | 在封存时提交解锁时间 | unlockAt 必须大于当前时间 |
| 自动解锁到期记录 | 定时任务 | 存在 SEALED 且到期记录 | 状态改为 UNLOCKED | 定时扫描并更新状态、写 unlockedAt | 必须幂等，避免重复解锁 |
| 发送解锁提醒 | 定时任务、微信消息服务 | 用户已授权订阅消息 | 产生提醒发送结果日志 | 解锁后调用微信服务发送提醒 | 发送失败不影响解锁主流程 |
| 查看已解锁记录 | 普通用户 | 已登录 | 返回已解锁分页列表 | 首页或快捷入口查询 UNLOCKED 数据 | 仅返回本人记录 |

### 4.3 核心业务流程图（顺序图：定时解锁）

```mermaid
sequenceDiagram
    participant Job as UnlockScheduler
    participant RS as RecordService
    participant DB as record表
    participant Log as unlock_notice_log表
    participant WX as 微信消息服务

    Job->>RS: 触发解锁任务
    RS->>DB: 查询 status=SEALED 且 unlockAt<=now
    DB-->>RS: 到期记录列表
    loop 每条记录
        RS->>DB: 更新 status=UNLOCKED, unlockedAt
        RS->>Log: 写入解锁日志(SUCCESS/FAILED)
        RS->>WX: 发送订阅提醒(已授权用户)
    end
```

---

## 5. 回看模块

### 5.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> RV1((查看已解锁记录详情))
    U --> RV2((查看当时元信息))
    U --> RV3((进行当时与现在对照))
```

### 5.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 查看已解锁记录详情 | 普通用户 | 已登录且记录归属本人 | 返回完整记录内容 | 查看标题、正文、类型、标签、状态等 | 非本人记录禁止访问 |
| 查看当时元信息 | 普通用户 | 记录存在 | 展示写下时上下文 | 展示写下日期、标签、核心问题、已过去天数 | 仅展示本人的记录元信息 |
| 当时与现在对照 | 普通用户 | 记录已解锁 | 完成一次回望 | 用户基于引导问题进行回顾 | 回看不强制触发回信 |

### 5.3 核心业务流程图（活动图：解锁后回看）

```mermaid
flowchart TD
    A([进入解锁详情页]) --> B{记录状态}
    B -->|UNLOCKED| C[展示正文与元信息]
    B -->|SEALED/DRAFT| D[提示暂不可回看]
    C --> E[展示引导问题\n这件事现在还重要吗]
    E --> F[用户完成当时vs现在对照]
    F --> G{是否回信}
    G -->|是| H[进入回信流程]
    G -->|否| I[结束回看]
```

---

## 6. 回信模块

### 6.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> RP1((创建回信))
    U --> RP2((查询回信))
```

### 6.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 创建回信 | 普通用户 | 已登录，记录归属本人且状态 UNLOCKED | 生成一条 reply | 提交 content + replyType 保存回信 | 同一记录只允许一条回信 |
| 查询回信 | 普通用户 | 已登录且记录归属本人 | 返回回信详情或 null | 查询指定记录回信 | 非本人记录禁止查询 |

### 6.3 核心业务流程图（顺序图：创建回信）

```mermaid
sequenceDiagram
    participant U as 普通用户
    participant API as ReplyController
    participant S as ReplyService
    participant R as record表
    participant RP as reply表

    U->>API: POST /api/records/{id}/reply
    API->>S: 创建回信
    S->>R: 校验记录归属与状态
    R-->>S: 记录信息(UNLOCKED)
    S->>RP: 校验是否已有回信
    RP-->>S: 不存在
    S->>RP: 插入reply
    RP-->>S: 保存成功
    S-->>API: 回信VO
    API-->>U: ApiResponse.success(data)
```

---

## 7. 标签模块

### 7.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> TG1((查询标签列表))
    U --> TG2((记录绑定标签))
```

### 7.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 查询标签列表 | 普通用户 | 已登录 | 返回启用标签列表 | 按 type 查询 MOOD/TOPIC 标签 | 仅返回 status=ENABLED 标签 |
| 记录绑定标签 | 普通用户 | 已登录且记录归属本人 | record_tag 关联生效 | 在创建/编辑草稿时提交 tagIds | 不允许绑定不存在标签 |

### 7.3 核心业务流程图（顺序图：查询标签）

```mermaid
sequenceDiagram
    participant U as 普通用户
    participant API as TagController
    participant S as TagService
    participant DB as tag表

    U->>API: GET /api/tags?type=MOOD
    API->>S: 查询标签
    S->>DB: 查询 status=ENABLED 且 type匹配
    DB-->>S: 标签列表
    S-->>API: 标签VO列表
    API-->>U: ApiResponse.success(data)
```

---

## 8. AI辅助模块

### 8.1 用例图

```mermaid
flowchart LR
    U[普通用户] --> AI1((获取写作提示))
    U --> AI2((整理记录内容))
    A[AI服务] --> AI1
    A --> AI2
```

### 8.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 获取写作提示 | 普通用户、AI服务 | 已登录 | 返回 prompts 列表 | 提交 recordType/coreQuestion/content 获取提示词 | AI 失败返回兜底提示 |
| 整理记录内容 | 普通用户、AI服务 | 已登录 | 返回 summary + coreQuestion | 提交 content 获取整理结果 | AI 超时或异常返回安全空结果 |

### 8.3 核心业务流程图（顺序图：AI写作提示） 

```mermaid
sequenceDiagram
    participant U as 普通用户
    participant API as AiController
    participant S as AiService
    participant M as 模型服务

    U->>API: POST /api/ai/writing-prompts
    API->>S: 生成写作提示
    S->>M: 调用模型接口
    alt 调用成功
        M-->>S: prompts
        S-->>API: 标准结果
    else 调用失败/超时
        S-->>API: 兜底prompts
    end
    API-->>U: ApiResponse.success(data)
```

---

## 9. 后台管理模块

### 9.1 用例图

```mermaid
flowchart LR
    A[管理员] --> AD1((管理员登录))
    A --> AD2((用户列表查询))
    A --> AD3((记录列表查询))
    A --> AD4((标签列表查询))
    A --> AD5((新增标签))
    A --> AD6((修改标签))
```

### 9.2 用例说明

| 用例 | 参与者 | 前置条件 | 后置条件 | 主成功场景 | 异常/约束 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 管理员登录 | 管理员 | 管理员账号可用 | 返回 admin token | 提交管理员账号密码登录 | DISABLED 状态禁止登录 |
| 用户列表查询 | 管理员 | 已登录后台 | 返回分页用户数据 | 按关键字/状态筛选 | 仅管理员可访问 |
| 记录列表查询 | 管理员 | 已登录后台 | 返回分页记录数据 | 按状态/类型/userId 筛选 | 管理端只读，不修改正文 |
| 标签管理 | 管理员 | 已登录后台 | 标签增改成功 | 新增/修改标签信息 | 标签名与类型需满足唯一约束 |

### 9.3 核心业务流程图（顺序图：管理员查询记录）

```mermaid
sequenceDiagram
    participant A as 管理员
    participant API as AdminRecordController
    participant S as AdminRecordService
    participant DB as record表

    A->>API: GET /admin/records?pageNum&pageSize&status
    API->>S: 后台记录查询
    S->>DB: 按条件分页查询
    DB-->>S: 分页结果
    S-->>API: 记录列表VO
    API-->>A: ApiResponse.success(pageData)
```

---

## 10. 说明与落地建议

- 图中状态流转严格采用：`DRAFT -> SEALED -> UNLOCKED`
- 回信规则固定：仅 `UNLOCKED` 且每条记录仅一条回信
- AI规则固定：失败可降级，不能成为主流程阻塞点
- 管理端权限与用户端权限严格隔离

以上可直接作为后续详细设计、接口测试用例、以及答辩展示的基础图稿。

---

## 11. 过程模型图（开发阶段）

本节用于梳理《时光回序》MVP从需求到验收的完整开发流程，便于项目排期、任务拆解、联调推进与阶段复盘。

### 11.1 开发总流程（活动图）

```mermaid
flowchart TD
    A([启动项目]) --> B[需求澄清与范围收敛]
    B --> C[MVP边界冻结与里程碑规划]
    C --> D[技术选型与工程骨架初始化]
    D --> E[数据库建模与建表脚本]
    E --> F[后端核心链路开发\n用户->记录->解锁->回信]
    F --> G[前端MVP页面开发]
    G --> H[前后端联调]
    H --> I[测试与缺陷修复]
    I --> J{是否达到验收标准}
    J -->|否| K[回到对应模块修复]
    K --> H
    J -->|是| L[上线准备与演示材料整理]
    L --> M([MVP验收完成])
```

### 11.2 跨成员协作流程（泳道图，可忽略）

成员分工：
- A：用户模块
- B：记录模块
- C：回信模块
- D：回看模块
- E：时间模块、AI模块、后台管理模块

```mermaid
flowchart TB
    subgraph PM[公共阶段]
        PM1[需求冻结与接口边界确认]
        PM2[数据库与统一规范确认]
        PM3[联调排期与验收标准确认]
    end

    subgraph ALane[成员A 用户模块]
        A1[实现注册 登录 获取当前用户 修改资料]
        A2[完成用户模块自测]
    end

    subgraph BLane[成员B 记录模块]
        B1[实现创建 编辑 删除草稿 封存 列表 详情]
        B2[完成记录模块自测]
    end

    subgraph ELane[成员E 时间 AI 后台]
        E1[实现自动解锁 已解锁查询]
        E2[实现AI提示与内容整理]
        E3[实现后台登录 用户记录标签管理]
        E4[完成时间 AI 后台模块自测]
    end

    subgraph DLane[成员D 回看模块]
        D1[实现解锁详情与当时现在对照展示]
        D2[完成回看模块自测]
    end

    subgraph CLane[成员C 回信模块]
        C1[实现创建回信 查询回信]
        C2[完成回信模块自测]
    end

    subgraph QA[联调与验收]
        Q1[前后端联调]
        Q2[回归测试与缺陷修复]
        Q3[MVP验收]
    end

    PM1 --> PM2 --> A1
    PM2 --> B1
    A2 --> B1
    B2 --> E1
    E4 --> D1
    E4 --> C1
    D2 --> Q1
    C2 --> Q1
    E4 --> Q1
    Q1 --> Q2 --> PM3 --> Q3
```

### 11.3 迭代工作流（状态图）

```mermaid
stateDiagram-v2
    [*] --> TODO: 需求进入待办池
    TODO --> DESIGNING: 方案设计中
    DESIGNING --> DEVELOPING: 开发中
    DEVELOPING --> TESTING: 提测
    TESTING --> FIXING: 发现缺陷
    FIXING --> TESTING: 修复后回归
    TESTING --> DONE: 回归通过
    DONE --> [*]
```

### 11.4 阶段产出与里程碑

| 阶段 | 核心目标 | 关键产出 | 验收要点 |
| ---- | ---- | ---- | ---- |
| 需求与方案阶段 | 明确做什么、不做什么 | 需求文档、设计文档、接口清单 | MVP边界是否清晰 |
| 工程初始化阶段 | 能启动、能连接、能鉴权 | 后端骨架、统一返回体、全局异常、JWT基础能力 | 基础接口可用 |
| 核心业务开发阶段 | 打通主链路 | 用户/记录/解锁/回信模块 | 状态流转正确且可复现 |
| 前端联调阶段 | 形成可演示产品 | 页面主流程、接口联调通过 | 端到端流程可走通 |
| 测试与收尾阶段 | 保证稳定与可交付 | 测试记录、缺陷清单、演示脚本 | 满足MVP验收5条标准 |

### 11.5 流程落地建议

- 以“主链路优先”推进开发：先保证 记录->封存->解锁->回看->回信 可完整演示。
- 每完成一个模块后立即做接口自测和联调，不把问题堆到最后统一处理。
- 用固定状态枚举和错误码约束实现，避免迭代中出现隐性返工。
- 测试阶段重点覆盖越权访问、状态越迁、重复提交与定时任务幂等场景。
