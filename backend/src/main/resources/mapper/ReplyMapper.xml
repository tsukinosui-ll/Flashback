<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flashback.mapper.ReplyMapper">

    <resultMap id="ReplyResultMap" type="com.flashback.domain.Reply">
        <id property="id" column="id"/>
        <result property="recordId" column="record_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="replyType" column="reply_type" javaType="com.flashback.domain.ReplyType"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.flashback.domain.Reply" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reply (
            record_id,
            user_id,
            content,
            reply_type,
            created_at
        ) VALUES (
            #{recordId},
            #{userId},
            #{content},
            #{replyType},
            #{createdAt}
        )
    </insert>

    <select id="selectByRecordId" resultMap="ReplyResultMap">
        SELECT
            id,
            record_id,
            user_id,
            content,
            reply_type,
            created_at
        FROM reply
        WHERE record_id = #{recordId}
        LIMIT 1
    </select>

</mapper>
