<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flashback.mapper.UnlockNoticeLogMapper">

    <insert id="insert" parameterType="com.flashback.domain.UnlockNoticeLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO unlock_notice_log (
            record_id,
            user_id,
            notice_type,
            notice_status,
            created_at
        ) VALUES (
            #{recordId},
            #{userId},
            #{noticeType},
            #{noticeStatus},
            #{createdAt}
        )
    </insert>

</mapper>
