# Backend Agent Rules

- Follow root `AGENTS.md`.
- Backend work is usually outside M1 unless an OpenSpec change explicitly includes it.
- Do not alter schema, migrations, status transitions, auth, notification behavior, or AI behavior unless the active task explicitly allows it.
- Always identify API contract impact before changing backend behavior.
- Prefer tests / type checks for backend changes when feasible.
